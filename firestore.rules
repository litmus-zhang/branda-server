rules_version = '2';

service cloud.firestore {
  match /databases/{database} {
    match /documents {
      match /users/{userId} {
        allow read, write: if request.auth.uid == userId;
        match /bookmarks {
          allow read, write: if request.auth.uid == userId;
        }
      }
    }
  }
}